extends layout

block content
  h1= title
  - let image_names = ['d1.jpg','d2.jpg','d3.jpg','d4.jpg','d5.jpg']
  - for(let i=0;i<3;i++){
      - let rno = Math.floor(Math.random()*5)
      - let iname = image_names[rno]

      img(src=`images/${iname}` alt='river' style='width:300px; height:300px;')
      |    
  - }